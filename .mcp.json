{
  "mcpServers": {
    "ai-vision-mcp": {
      "type": "stdio",
      "command": "npx",
      "args": ["ai-vision-mcp"],
      "env": {
        "IMAGE_PROVIDER": "google",
        "VIDEO_PROVIDER": "google",
        "GEMINI_API_KEY": "${GEMINI_API_KEY}"
      }
    }
  }
}
